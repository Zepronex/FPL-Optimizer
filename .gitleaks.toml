[extend]
useDefault = true

[allowlist]
description = "Generated dependency installations are audited from manifests and lockfiles, not vendored copies."
paths = [
  '''(^|/)node_modules/''',
  '''(^|/)\.venv/''',
  '''(^|/)\.git/'''
]
