# Web app configuration
# The current web client uses relative /api requests. Configure production rewrites
# or update the client before relying on this variable for separate-origin APIs.
VITE_API_BASE_URL=/api
VITE_APP_NAME=ScoutIQ
